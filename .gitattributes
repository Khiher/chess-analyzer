# Normalize line endings to LF for all text files so formatting is consistent
# across Windows/macOS/Linux and CI (prettier is configured for LF).
* text=auto eol=lf

# Binary assets should never be line-ending normalized.
*.png binary
*.jpg binary
*.ico binary
*.icns binary
*.exe binary
*.dmg binary
*.AppImage binary
